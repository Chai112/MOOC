import 'package:flutter/material.dart'; // Flutter

class ScholarlyDivider extends StatelessWidget {
  // constructor
  const ScholarlyDivider({Key? key}) : super(key: key);

  // main build function
  @override
  Widget build(BuildContext context) {
    return const Divider(
      thickness: 1,
    );
  }
}
